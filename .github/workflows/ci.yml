name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: ${{ matrix.os }} / Node ${{ matrix.node }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            node: 22
          - os: ubuntu-latest
            node: 24
          - os: ubuntu-latest
            node: 26
          - os: windows-latest
            node: 24
          - os: macos-latest
            node: 24
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node }}
          package-manager-cache: false

      - name: Print tool versions
        run: |
          node --version
          npm --version

      - name: Run test suite
        run: npm test

      - name: CLI --version
        run: node bin/ddae-engine.js --version

      - name: CLI --help
        run: node bin/ddae-engine.js --help

      - name: Validate package (npm pack --dry-run)
        run: npm pack --dry-run

      - name: Verify working tree is clean
        run: node scripts/ci/verify-clean-tree.mjs
